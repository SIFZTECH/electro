import CreateNewAttributeValue from "./CreateNewAttributeValue";

export const metadata = {
  title: "Leon Cycle | Create New Attribute Value",
};

const page = () => {
  return <CreateNewAttributeValue />;
};

export default page;
